{
  "name": "high-throughput-inventory-tracker",
  "version": "1.0.0",
  "description": "Flash-sale inventory tracker backed by Redis atomic operations.",
  "main": "src/server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "load-test": "node scripts/load-test.js"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "redis": "^4.7.0"
  }
}
