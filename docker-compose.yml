services:
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

  api:
    build: .
    depends_on:
      - redis
    environment:
      PORT: 3000
      REDIS_URL: redis://redis:6379
      INVENTORY_KEY: flash-sale:inventory
      INITIAL_STOCK: 500
    ports:
      - "3000:3000"
